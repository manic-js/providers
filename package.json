{
  "name": "@manicjs/providers",
  "version": "1.0.2-canary.0",
  "description": "Deployment providers for Manic framework - Vercel, Cloudflare, Netlify",
  "keywords": [
    "adapter",
    "cloudflare",
    "deploy",
    "manic",
    "netlify",
    "vercel"
  ],
  "homepage": "https://github.com/manic-js/providers#readme",
  "bugs": {
    "url": "https://github.com/manic-js/providers/issues"
  },
  "license": "GPL-3.0",
  "author": "Rahuletto",
  "repository": {
    "type": "git",
    "url": "https://github.com/manic-js/providers"
  },
  "files": [
    "src/**/*",
    "README.md"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./vercel": {
      "types": "./src/vercel/index.ts",
      "import": "./src/vercel/index.ts"
    },
    "./cloudflare": {
      "types": "./src/cloudflare/index.ts",
      "import": "./src/cloudflare/index.ts"
    },
    "./netlify": {
      "types": "./src/netlify/index.ts",
      "import": "./src/netlify/index.ts"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "ci:lint": "bunx --bun oxlint --config .oxlintrc.json .",
    "ci:format": "bunx --bun oxfmt --check --config .oxfmt.json .",
    "ci:typecheck": "if [ -f tsconfig.json ]; then bunx --bun tsc --noEmit; elif [ -f src/index.ts ]; then bunx --bun tsc --noEmit --module esnext --moduleResolution bundler --target es2022 src/index.ts; elif [ -f index.ts ]; then bunx --bun tsc --noEmit --module esnext --moduleResolution bundler --target es2022 index.ts; else echo \"No TypeScript entrypoint found\"; exit 1; fi",
    "ci:build": "if bun run | grep -q \"^ build$\"; then bun run build; elif [ -f src/index.ts ]; then bun build src/index.ts --target bun --outdir .ci-build; elif [ -f index.ts ]; then bun build index.ts --target bun --outdir .ci-build; else echo \"No build entrypoint found\"; exit 1; fi",
    "ci:test": "if bun run | grep -q \"^ test$\"; then bun run test; elif [ -d tests ] || ls -1 *.test.* >/dev/null 2>&1 || ls -1 **/*.test.* >/dev/null 2>&1; then bun test; else echo \"No tests found; passing by policy\"; fi",
    "ci:compliance": "test -f CODE_OF_CONDUCT.md && test -f LICENSE && test -f README.md"
  },
  "dependencies": {
    "@manicjs/tui": "1.0.2-canary.0"
  },
  "devDependencies": {
    "@shikijs/transformers": "^4.0.2",
    "@types/bun": "^1.3.13",
    "@types/mdx": "^2.0.13",
    "fumadocs-docgen": "^3.0.10",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {},
  "peerDependenciesMeta": {}
}
